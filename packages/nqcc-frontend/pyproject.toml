[project]
name = "nqcc-frontend"
version = "0.1.0"
description = "Frontend for nqcc"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nqcc"]
